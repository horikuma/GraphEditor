# GraphEditor

SwiftUI で作った macOS 用のシンプルな円描画アプリです。

## 使い方

1. `GraphEditor.xcodeproj` を Xcode で開く
2. `GraphEditor` スキームを実行する
3. キャンバス上でマウスドラッグして円を描く

上部ツールバーから線色、線幅、塗りつぶしの有無を変更できます。

## キーボード操作

- 左右キー: 操作軸を選択
- 上下キー: 現在の操作軸に対して線形操作

操作軸は、図形選択、X軸座標、Y軸座標、Width、Height です。現在の操作軸と値はキャンバス左下に表示されます。
